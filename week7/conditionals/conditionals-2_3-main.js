function differenceNumber(number) {
  if (number > 27) {
    const result = (number - 27) * 2;
    console.log("bigger than 27. Result:" + result);
  } else {
    console.log("Number Smaller than 27. Number entered: " + number);
  }
}
